{
  "name": "api-gateway",
  "version": "0.0.1",
  "description": "Gateway API for Magnates Front",
  "main": "server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=development nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "http-proxy-middleware": "^2.0.6",
    "nodemon": "^3.0.2"
  }
}
